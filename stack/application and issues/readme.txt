- Hint: when using recursive function the os is responsible for buiding the stack.

( iterative or recursive ) ???

- solving the problem iteratively is better if the iterative algorithm does not need a stack.
  Hence, solving it recursively builds an unnecessarily stack,hich wastes memory and time consumed in function return.
 
- if i will solve the problem iteratively using stack, it's better to using recursive way, as OS is better than me
